# 15. 三数之和

给你一个包含 n 个整数的数组 nums，判断 nums 中是否存在三个元素 a，b，c ，使得 a + b + c = 0 ？请你找出所有满足条件且不重复的三元组。

**注意：**答案中不可以包含重复的三元组。

示例：

```
给定数组 nums = [-1, 0, 1, 2, -1, -4]，

满足要求的三元组集合为：
[
  [-1, 0, 1],
  [-1, -1, 2]
]
```

思路：

```
1：暴力解法
三重循环分别找出第一，第二，第三个数，然后算出和是否为0
时间复杂度：O（n^3）
为避免重复解，先排序，再从左到右的去寻找答案
2：双指针法
不包含重复解——模式识别——排序按照一定顺序寻找
从已经排序好的序列中，找到两个数之和等于一个特殊值
利用双指针找到

先将数组排序
判断不

3：判断特殊值
举例：0，0,0,0

```

代码思路：

```java


```





